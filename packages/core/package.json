{
	"name": "integration-mock-core",
	"version": "0.1.0",
	"private": true,
	"description": "integration-mock internals: pack model, matcher, layered resolution, request log, faults, snapshot build/diff (bundled into the integration-mock CLI; not published)",
	"license": "MIT",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"ajv": "^8.17.1",
		"nanoid": "^5.0.7",
		"picomatch": "^4.0.2",
		"undici": "^6.19.0"
	},
	"devDependencies": {
		"@types/picomatch": "^3.0.1",
		"yaml": "^2.5.0"
	},
	"files": [
		"dist"
	]
}
