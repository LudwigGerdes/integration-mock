## What changed and why

<!-- One or two sentences from the user's point of view. -->

## Checklist

- [ ] A test fails without this change and passes with it (`pnpm build && pnpm test`)
- [ ] No `any`, no `as` where a type guard would do; `pnpm typecheck` is clean
- [ ] Commit messages follow Conventional Commits, and CHANGELOG.md has an entry if a user would notice
